#include <stdio.h>
#include <stdlib.h>

// read which rom to load from cli
// call read_rom on opened file
// close file, save buffer,
// note: probably edit buffer code to load program straight into memory
// call cpu init, execute etc
// figure out how to link display
// figure out timers

int main(int argc, char* argv[]) {
    
}
